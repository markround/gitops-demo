---
apiVersion: v1
kind: Service
metadata:
  name: nginx
  namespace: default
  labels:
    name: nginx
spec:
  type: ClusterIP
  ports:
    - port: 80
      name: http
      protocol: TCP
  selector:
    app: nginx
